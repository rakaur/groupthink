<div class="dropdown-item">
  <div class="field">
    <%= f.label :created, "created at", class: "label" %>
    <p class="control has-icons-left has-icons-right">
      <%=
        f.text_field :created,
                     type: "date",
                     autofocus: false,
                     placeholder: "date created",
                     class: "input",
                     value: stream.created&.to_date,
                     data: {
                       "filter-target" => "input",
                       "panel-target"  => "input",
                       "action"        => "filter#inputChanged"
                     }
      %>
      <span class="icon is-small is-left">
        <i class="fas fa-calendar"></i>
      </span>
      <span class="icon is-small is-right">
        <button class="delete is-small" data-action="click->filter#clearInput"></button>
      </span>
      <p class="help is-info">
        press delete, backspace, or press the
        <i class="delete is-tiny" data-action="click->filter#clearInput"></i>
        to remove this filter
      </p>
    </p>
  </div>
</div>
<hr class="dropdown-divider">
<div class="dropdown-item">
  <p class="has-text-weight-bold is-italic is-size-6">created &lt;datetime&gt;</p>
</div>
<hr class="dropdown-divider">
<div class="dropdown-item">
  <p class="help is-size-6">
    the created filter limits the results to those created on a particular date,
    regardless of how many were found by other filters
  </p>
</div>
